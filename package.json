{
  "name": "template_vanilla",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "update": "tsc && vite build && butler push ./dist notaninart/note-mapping:html",
    "ghpages_build": "tsc && vite build --mode include_frame"
  },
  "devDependencies": {
    "typescript": "^5.3.3",
    "vite": "^4.5.3"
  },
  "dependencies": {
    "lil-gui": "^0.18.2"
  }
}
